= form_for :video, :url => videos_path, :html => { :multipart => true, :method => :post } do |form|
	%p= form.text_field :title, :title => 'Video title'
	%p= form.text_area :description, :title => 'Video description'
	%p= form.submit "Submit"