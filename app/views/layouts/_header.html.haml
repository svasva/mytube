#logo
	%a.logo(href='/') MyTube
#nav
	#menu
		= link_to 'Upload new', '/videos/new' if user_signed_in?
	- if user_signed_in?
		Signed in as
		%email #{current_user.email}
		| #{link_to "Sign out", destroy_user_session_path}
	- else
		#{link_to "Sign up", new_user_registration_path} or #{link_to "Sign in", new_user_session_path}
