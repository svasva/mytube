#logo
	%a.logo(href='/') MyTube
#nav
	#menu
		= link_to 'Home', '/'
		- if user_signed_in?
			|
			%a.upload(href='#') Upload video
			%span.upload
				%input(type='file' id='video_source')
			|
			= link_to 'My Videos', '/videos/my'
			#usernav
				Signed in as
				%email #{current_user.email}
				| #{link_to "Sign out", destroy_user_session_path}
		- else
			#usernav
				#{link_to "Sign up", new_user_registration_path} or #{link_to "Sign in", new_user_session_path}
